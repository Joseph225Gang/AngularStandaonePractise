import { OverHighlightDirective } from './over-highlight.directive';

describe('OverHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new OverHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
